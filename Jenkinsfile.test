pipeline {
    agent any
    triggers {
//        githubEventTrigger(eventName: "label", eventPayloadFilter: ["action": "created"], triggerName: "Label Created")
        githubEventTrigger(eventName: "pull_request", eventPayloadFilter: ["action": "closed"], triggerName: "PR Closed")
    }
    stages {
        stage("Test") {
            steps {
                echo "Event: " + env.GITHUB_EVENT_NAME
                echo "Trigger: " + env.GITHUB_EVENT_TRIGGER_NAME
            }
        }
    }
}
