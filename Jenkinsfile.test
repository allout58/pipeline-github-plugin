pipeline {
    agent any
    triggers {
        githubEventTrigger("label", ["action": "created"], "Label Created")
    }
    stages {
        stage("Test") {
            echo "Event:" + env.GITHUB_EVENT_NAME
            echo "Trigger" + env.GITHUB_EVENT_TRIGGER_NAME
        }
    }
}
